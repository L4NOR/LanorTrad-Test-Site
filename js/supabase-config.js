/* =========================================================================
   LanorTrad — Configuration Supabase (forum + comptes).
   ⚙️  À REMPLIR : colle ici les 2 valeurs de ton projet Supabase
       (Project Settings → API → "Project URL" et "anon public").
   ℹ️  La clé "anon" est PUBLIQUE par conception : elle est faite pour vivre
       dans le navigateur. Ce sont les règles RLS (voir supabase/schema.sql)
       qui protègent réellement les données. Tu peux donc la committer.
   ========================================================================= */
window.LT_SUPABASE = {
  url:     "https://VOTRE_PROJET.supabase.co",   // ← Project URL
  anonKey: "VOTRE_CLE_ANON_PUBLIC"               // ← clé "anon public"
};
